"Enable syntax coloration
syntax on

"Switch to a color scheme more adapted to dark backgrounds,
"comment to use for light backgrounds
set background=dark

"Set tab width to 4
set tabstop=4
set shiftwidth=4

"When hitting tab key, input spaces instead
set expandtab

"Display line numbers
set number

"Enable mouse controls (click, etc)
set mouse=a
